coordinates = (4,5)
# Tuples are immutable - can't change- add, modify, extend like the lists.
# if we just replace the first parenthesis with the third ones, we get lists.
# coordinates[1] = 10 ----->  won't work
print(coordinates[0])

# List of tuples:
coordinates2 = [(4,5), (5,6), (6,7)]
print(coordinates2)